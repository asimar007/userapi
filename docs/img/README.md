# Screenshot images

Drop your demo screenshots here using these exact filenames so they render in
the README and docs/SCREENSHOTS.md:

- 01-docker-up.png   — `docker compose up` logs (server started)
- 02-create.png      — POST /users returning 201
- 03-get-age.png     — GET /users/1 showing the computed age
- 04-list.png        — GET /users with pagination + request-id headers
- 05-update.png      — PUT /users/1 returning 200
- 06-delete.png      — DELETE /users/2 returning 204
- 07-errors.png      — 400 / 404 error responses
- 08-tests.png       — go test ./... output

PNG or JPG both work (update the extension in the markdown if you use .jpg).
